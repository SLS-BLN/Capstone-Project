export default function ItemList(displayedItems) {
  return (
    <div>
      <h2>Hello World</h2>
      {/* {displayedItems.map(item => (
        <div key={item.id}>
          <h3>{item.tag}</h3>
        </div>
      ))} */}
    </div>
  );
}
